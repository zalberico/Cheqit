<%= form_for current_user.relationships.build(:cheqed_id => @user.id), 
             :remote => true do |f| %>
  <div><%= f.hidden_field :cheqed_id %></div>
  <div class="actions"><%= f.submit "cheq" %></div>
<% end %>
